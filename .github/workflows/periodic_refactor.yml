name: Automated Code Refactoring

on:
  # Manual trigger only. This job force-pushes the refactored-code branch and opens a PR,
  # so the weekly cron ('0 0 * * 1') is left commented out until you opt in.
  # schedule:
  #   - cron: '0 0 * * 1'
  workflow_dispatch:

jobs:
  refactor:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install python-dotenv
          pip install google-generativeai
          pip install GitPython
          pip install requests
          
      - name: Run refactoring pipeline
        run: python docs/llm_pipeline/script.py
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          USERNAME: ${{ secrets.USERNAME }}
          
      - name: Handle errors
        if: failure()
        run: |
          echo "Refactoring pipeline failed. Check the logs for more details."