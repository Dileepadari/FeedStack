Analysis Summary:
Total Java files: 30
Files analyzed: 1
Files skipped or errored: 29

Detailed Analysis:
Single File 4/8:
File: ./repository/reader-web/src/main/java/com/sismics/reader/rest/resource/UserResource.java
Smells Found:
[
  {
    "type": "Data Class",
    "category": "design",
    "description": "`User` and `AuthenticationToken` are simple data classes with no behaviors.",
    "files": [
      "both"
    ],
    "fix": "Consider making them immutable and using a builder pattern for creating instances."
  },
  {
    "type": "Lazy Initialization",
    "category": "code",
    "description": "The `authenticationTokens` field in `User` is lazily initialized, which can lead to unexpected behavior.",
    "files": [
      "User.java"
    ],
    "fix": "Consider making the field eager-initialized."
  },
  {
    "type": "Magic Numbers",
    "category": "code",
    "description": "The `@Column(length = 50)` and `@Column(length = 256)` annotations use magic numbers for the column lengths.",
    "files": [
      "User.java",
      "AuthenticationToken.java"
    ],
    "fix": "Replace the magic numbers with meaningful constants."
  },
  {
    "type": "Long Method",
    "category": "code",
    "description": "The `equals` and `hashCode` methods in both `User` and `AuthenticationToken` are too long and complex.",
    "files": [
      "User.java",
      "AuthenticationToken.java"
    ],
    "fix": "Extract the common code into helper methods or use a library for generating equals and hashCode methods."
  },
  {
    "type": "Nested Builder Class",
    "category": "design",
    "description": "The `Builder` classes in both `User` and `AuthenticationToken` are nested within the main class, which makes the code harder to read and maintain.",
    "files": [
      "User.java",
      "AuthenticationToken.java"
    ],
    "fix": "Move the `Builder` classes to separate files."
  }
]

