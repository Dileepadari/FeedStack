Analysis Summary:
Total Java files: 30
Files analyzed: 3
Files skipped or errored: 27

Detailed Analysis:
Pair 8/11:
Files:
- ./repository/reader-web-common/src/main/java/com/sismics/util/filter/SecurityFilter.java
- ./repository/reader-web-common/src/main/java/com/sismics/util/filter/TokenBasedSecurityFilter.java
Smells Found:
[
  {
    "type": "Cyclic dependency",
    "category": "design",
    "description": "The two files depend on each other.",
    "files": [
      "file1",
      "file2"
    ],
    "fix": "Extract common functionality into a separate module or library."
  },
  {
    "type": "God class",
    "category": "code",
    "description": "The SecurityFilter class has too many responsibilities.",
    "files": [
      "file1"
    ],
    "fix": "Break the class into smaller, more focused classes."
  },
  {
    "type": "Long method",
    "category": "code",
    "description": "The doFilter method is too long.",
    "files": [
      "file1"
    ],
    "fix": "Extract the parts of the method that are not directly related to the core functionality into separate methods."
  },
  {
    "type": "Duplicated code",
    "category": "code",
    "description": "The injectAuthenticatedUser and injectAnonymousUser methods are very similar.",
    "files": [
      "file1"
    ],
    "fix": "Extract the common functionality into a separate method."
  }
]

Single File 4/8:
File: ./repository/reader-web/src/main/java/com/sismics/reader/rest/resource/UserResource.java
Smells Found:
[
  {
    "type": "Long Parameter List",
    "category": "code",
    "description": "The constructor of the `User` class has 14 parameters, which is considered excessive and difficult to maintain.",
    "files": [
      "file1"
    ],
    "fix": "Use the builder pattern to create instances of the `User` class, or split the constructor into multiple methods with smaller parameter lists."
  },
  {
    "type": "Lazy Initialization",
    "category": "code",
    "description": "The `authenticationTokens` field is initialized to a new `HashSet` instance in the constructor, but this initialization is unnecessary and can be removed.",
    "files": [
      "file1"
    ],
    "fix": "Remove the initialization of the `authenticationTokens` field in the constructor."
  },
  {
    "type": "Complex Data Structure",
    "category": "design",
    "description": "The `User` class has a complex data structure, with multiple fields and a one-to-many relationship with the `AuthenticationToken` class.",
    "files": [
      "file1"
    ],
    "fix": "Consider refactoring the `User` class to make it more cohesive and easier to maintain, perhaps by creating separate classes for different aspects of the user's data."
  }
]

