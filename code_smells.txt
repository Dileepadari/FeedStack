Analysis Summary:
Total Java files: 17
Files analyzed: 3
Files skipped or errored: 14

Detailed Analysis:
Pair 1/6:
Files:
- ./repository/reader-web/src/main/java/com/sismics/reader/rest/resource/AllResource.java
- ./repository/reader-web/src/main/java/com/sismics/reader/rest/resource/AppResource.java
Smells Found:
[
  {
    "type": "God Class",
    "category": "design",
    "description": "The `AllResource` class has too many responsibilities, including getting articles, marking articles as read, and returning information about the application.",
    "files": [
      "file1"
    ],
    "fix": "Extract the different responsibilities into separate classes."
  },
  {
    "type": "Shotgun Surgery",
    "category": "design",
    "description": "The `read()` method is responsible for both marking all articles as read and updating the unread count for each feed subscription.",
    "files": [
      "file1"
    ],
    "fix": "Extract the updating of the unread count for each feed subscription into a separate method."
  },
  {
    "type": "Long Parameter List",
    "category": "code",
    "description": "The `get()` method has a long parameter list, making it difficult to read and understand.",
    "files": [
      "file1"
    ],
    "fix": "Use a request object to encapsulate the parameters."
  },
  {
    "type": "Deeply Nested Code",
    "category": "code",
    "description": "The `log()` method contains deeply nested code, making it difficult to follow the logic.",
    "files": [
      "file2"
    ],
    "fix": "Extract the nested code into separate methods."
  },
  {
    "type": "Unhandled Exception",
    "category": "code",
    "description": "The `version()` method does not handle the `JSONException` that can be thrown when creating the response.",
    "files": [
      "file2"
    ],
    "fix": "Add exception handling to the `version()` method."
  }
]

