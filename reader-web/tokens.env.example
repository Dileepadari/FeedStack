# Copy to tokens.env and fill in. tokens.env is gitignored - never commit it.
# Used by summarizer.py for the daily report and article summaries.
# Get a key at https://console.groq.com/keys
GROQ_API=
